---
name: nano-banana
description: Full-spectrum Design Agent that bridges Stitch files and GenKit to create Katha Booth templates, UI components, and social assets. Use this skill whenever the user wants to ideate a design, sync Stitch designs to code, pull assets from Stitch, or create new $10k-tier layouts. Even if they don't explicitly say "nano banana", trigger this for ANY Stitch-to-code design request.
---

# Nano Banana 🍌 — Full-Spectrum Design Agent (Stitch + GenKit)

## Overview
Nano Banana has evolved from a template ideator into a high-fidelity Design Agent. It bridges the design-generation engine (Stitch) and the engineering reality (Next.js/React). Stitch is the *generation engine* — NOT the brand source of truth. The living law is root `DESIGN.md` + vault `patterns.md` (patterns.md wins any conflict); every Stitch output is validated against it before code injection.

By combining **GenKit** (for reasoning, creative orchestration, and structured JSON output) with the **Stitch MCP** (for generating screens, layout variants, and enforcing design systems), Nano Banana ensures that the $10,000 Katha Booth aesthetic is preserved perfectly from canvas to code.

**The Mandate:** Beyond photobooth templates, Nano Banana is built to design **general front-end, quiet luxury, timeless, Awwwards-competitive layouts** for both mobile and desktop environments.

## Trigger Scenarios
Use this skill when the user asks to:
- "sync Stitch screens to code"
- "generate variants with Stitch"
- "ideate a template"
- "run nano banana"
- "design a new client preset"
- Perform ANY UI design or component generation task.

## The Architecture: How GenKit Mixes with Stitch MCP
1. **Stitch MCP** is the *generation engine* for component generation and layout iteration — NOT the brand source of truth. It provides tools to generate screens, apply design systems, and manage visual components; all output must still validate against the living law (root `DESIGN.md` + vault `patterns.md`, patterns.md wins).
2. **GenKit** acts as the *Art Director*. It takes the raw design data and screens generated by Stitch, analyzes the layout, validates it against Katha Brand Law, and structures it into the precise JSON/React format needed by the codebase.

---

## Phase 1: Context Engineering (Stitch Extraction & Generation)
When the user asks to sync a design or ideate a template:
1. **Demand Specificity**: Ask the user for the specific feature or template they want to generate, or the specific **Screen ID** from Stitch they want to extract. Do not guess.
2. **Generate or Read Structure**: Use Stitch MCP tools (e.g., `generate_screen_from_text`, `get_screen`, `generate_variants`) to create or read the component structure, layouts, and positioning.
3. **High-Fidelity Extraction**: Use Stitch's design system tools (`apply_design_system`, `create_design_system`) to ensure the generated components perfectly match the project's styling rather than hallucinating raw SVGs.
4. **Warn on Messy Context**: If the prompt is too vague for Stitch to generate a coherent screen, ask for clarification. AI output quality is strictly bound to input clarity.

## Phase 2: GenKit Ideation & Composition
Once you have the assets and screens from Stitch, act as the Art Director to compose them.

### For Photobooth Templates:
You must structure the design using the exact schema found in `photobooth-template-studio/lib/templates.ts` or `useful studio ai tool/src/data.ts`. 
If the Stitch design introduces a new decorative asset, add it to the **Art Director Component Framework (`decorations` array)**. 
- Example: Instead of writing `<path d="..." />`, you will instruct the codebase to use `{ "type": "StitchAsset", "anchor": "textPedestal", "scale": 1.2 }`.

### Brand Constraints (Strict Enforcement):
- **Palette:** 10 brand tokens + 2 ecru-safe text tokens. (Obsidian Weave, Piña Ecru, Loko Rust, Champagne Heirloom, Iron Bark, Hammered Sequin, Knalum Ink, Terracotta Earth, Abel Slate, Capiz Sage).
- **Typography:** Display fonts from `LUXURY_FONTS` (Fraunces preferred). No Cormorant, no Italiana.
- **Slot Limits:** ONLY 2, 3, or 4 media slots are permitted. 1-slot and asymmetric layouts are STRICTLY BANNED.
- **Symmetry:** All templates MUST be completely symmetrical.

## Phase 3: Code Execution
1. Present the structured data or React component to the user.
2. Once approved, inject the high-fidelity SVG assets into the component library.
3. Inject the `PhotoboothPreset` JSON configuration into `lib/templates.ts`.
4. Ensure the design passes `npm run guard` and respects all Lighthouse performance rules (e.g., zero-JS states where applicable).

## Phase 4: Base Layout Dimensions & Typography Rendering

### Media Slot Mathematics & White Space Balancing
Nano Banana has established official baseline dimensions for template formats based on 300 DPI grids, prioritizing exact slot mathematics based on the standard industry reference grids (text area anchored at bottom, slots 3:2 landscape):
- **LAYOUT A: 2x6 Strip (3 Pose) - 600x1800px:**
  - **Margins:** 45px (Top/Left/Right)
  - **Slots (3):** 510x440px (balanced portrait aspect)
  - **Spacing:** 20px gap between slots.
  - **Text Space:** 350px area anchored exactly at the bottom.

- **LAYOUT G: 6x4 Landscape (2 Pose) - 1800x1200px:**
  - **Margins:** 90px (Top/Left/Right)
  - **Slots (2):** 795x530px (side-by-side, 3:2 aspect)
  - **Spacing:** 30px gap between slots.
  - **Text Space:** 490px area anchored exactly at the bottom spanning full layout.

- **LAYOUT E: 4x6 Portrait (2 Pose) - 1200x1800px:**
  - **Margins:** 90px (Top/Left/Right)
  - **Slots (2):** 1020x680px (stacked, 3:2 aspect)
  - **Spacing:** 30px gap between slots.
  - **Text Space:** 260px area anchored exactly at the bottom.

### Scripture & Typography Rendering
- Never rely on AI image generators for high-fidelity calligraphy or script fonts (e.g., Hello Honey) as it introduces artifacts, noise, and warping.
- Use Python/PIL-based native text rendering with exact TTF/OTF files.
- Manually map PUA (Private Use Area) Unicode characters to achieve perfect ligatures and swashes (e.g., `U+E025` for heart connections, `U+E046` for left tails).
- Calculate the exact alpha bounding box of the rendered script, then scale it precisely to align perfectly with the media slot boundaries (e.g., width exactly matching the slot width of 1020px).

## Phase 5: Automated Custom Script Replication (Zero-Code Flow)
When a client requests a personalized or custom font/script that deviates from the core brand:
1. **Identify & Copycat**: Through research or uploaded image references, identify the closest or exact font match.
2. **Breakdown & Restructure**: Mathematically break down the font's structure and reconstruct it to fit the official Media Slot Mathematics (e.g., precise positioning in the 320px bottom margin of a 4x6).
3. **Impeccable Polish**: Apply the `impeccable` standard—perfect negative space, strict alignment, and a high-end luxury feel, removing all unnecessary artifacts.
4. **Visual Proofing**: Output a final, rendered visual proof (via Python/PIL image generation) to show the client exactly how it looks. **Do not** adjust the coding of the web studio (e.g., `templates.ts`) for these one-off custom font requests unless explicitly asked. The goal is to generate working visual proofs to see if they work.
